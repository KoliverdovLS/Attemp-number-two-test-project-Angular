import { GetSumAmountByUsersArrPipe } from './get-sum-amount-by-users-arr.pipe';

describe('GetSumAmountByUsersArrPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSumAmountByUsersArrPipe();
    expect(pipe).toBeTruthy();
  });
});
